apiVersion: v1
kind: Pod
metadata:
  name: gpu-validation
  labels:
    app: gpu-validation
spec:
  restartPolicy: Never
  containers:
    - name: cuda
      image: nvidia/cuda:12.4.1-base-ubuntu22.04
      command: ["nvidia-smi"]
      resources:
        limits:
          nvidia.com/gpu: 1
