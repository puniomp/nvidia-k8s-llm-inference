apiVersion: v1
kind: Pod
metadata:
  name: gpu-pending
  labels:
    experiment: scheduling
spec:
  restartPolicy: Never
  containers:
    - name: cuda
      image: nvidia/cuda:12.4.1-base-ubuntu22.04
      command: ["nvidia-smi"]
      resources:
        limits:
          nvidia.com/gpu: 2
